Suite Sweet {
    BeforeAll {
        start("firefox");
        navigate("https://arcor.com/es/nuestras-marcas");
    }

    Module Mogul {
        $("//h2[text()='MOGUL']").click(); 
        $("//h2[text()='MOGUL']").click(); 
        assertTrue $("//div[@class='text']/p").text().contains("celíacos");
        $("]]]]").click(); // If the xpath is invalid, it is JavaScript's problem 

        // wrong type is not validated in syntax, but in semantics
        // TODO: Move this part of the test to a new reject test
        $("//*").click(); 
        $(1).click();
        $({}).click();
        $(() => {return "//*";}).click();

        #("//*").forEach((elem) => {elem.click();});
        #("//h2*")[0].click();
    }

    AfterAll {
        var a = "//*";
        $(a).click();
        #(a).click();
        $((a)).click();
        $((a).toUpper());
    }
}
